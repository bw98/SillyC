首次适应算法指的是从空闲分区表中从小到大顺序排序空闲区，然后进行查表，将最先满足内存大小的空闲区分给作业。这样做的话有利于查找，且给大作业留有空间；但是会在一些分区中留下小的内存碎片而无法利用。在代码实现中，使用了两个链表，一个作为空闲区块的“串”，另一个作为已分配内存块的“串”，在杀死进程的时候，利用冒泡排序重新排列内存块以提高效率，但是分配时仍有 bug，比如无法调整内存大小（因为数据结构是固定的），并且只有写了简单的单张表而没有考虑到多张表，希望在接下来的学习中进一步改正